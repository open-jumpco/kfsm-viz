buildscript {
    dependencies {
        classpath group: 'org.jetbrains.kotlinx', name: 'kotlinx-html-jvm', version: '0.7.3'
    }
}
plugins {
    id 'java'
    id 'maven-publish'
    id 'signing'
    id 'org.jetbrains.kotlin.jvm' version '1.5.10'
    id 'org.jetbrains.dokka' version '1.5.0'
}

group 'io.jumpco.open'
version '1.5.2'

apply from: "$rootProject.projectDir/gradle/publish.gradle"

sourceCompatibility = 1.8

repositories {
    mavenLocal()
    mavenCentral()
}

configurations {
    fatjar
}

dependencies {
    implementation 'org.jetbrains.kotlin:kotlin-stdlib-jdk8'
    compileOnly 'org.jetbrains.kotlin.spec.grammar.tools:kotlin-grammar-tools:0.1-OPEN-JUMPCO'
    fatjar('org.jetbrains.kotlin.spec.grammar.tools:kotlin-grammar-tools:0.1-OPEN-JUMPCO') {
        exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib'
    }
    testCompile 'org.jetbrains.kotlin.spec.grammar.tools:kotlin-grammar-tools:0.1-OPEN-JUMPCO'
    testCompile group: 'junit', name: 'junit', version: '4.13'
    testCompile 'io.jumpco.open:kfsm-jvm:1.5.1'
}

jar {
    duplicatesStrategy 'exclude'
    from {
        configurations.fatjar.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
}

compileKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
compileTestKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
